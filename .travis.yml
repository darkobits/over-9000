language: node_js
node_js: 8

install:
  - npm install

script:
  - npm run build
  - npm test -- --coverage --silent

after_success:
  - npm install codacy-coverage
  - $(npm bin)/codacy-coverage -v < ./coverage/lcov.info

cache:
  directories:g
    - node_modules
